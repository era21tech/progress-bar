# progress-bar
A lightweight circular progress component that animates an SVG ring to a target percentage while counting the numeric percentage in the center — pure HTML, CSS and vanilla JavaScript.
